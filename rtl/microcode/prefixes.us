// ES segment override
.at 0x26;
    segment ES, prefix_type SEGMENT_OVERRIDE, fifo_pop, jmp_opcode;

// CS segment override
.at 0x2e;
    segment CS, prefix_type SEGMENT_OVERRIDE, fifo_pop, jmp_opcode;

// SS segment override
.at 0x36;
    segment SS, prefix_type SEGMENT_OVERRIDE, fifo_pop, jmp_opcode;

// DS segment override
.at 0x3e;
    segment DS, prefix_type SEGMENT_OVERRIDE, fifo_pop, jmp_opcode;
